{
  "routes": [
    {
      "method": "POST",
      "path": "/accounts/waitlist",
      "handler": "Accounts.waitlist",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/accounts/newsletter",
      "handler": "Accounts.newsletter",
      "config": {
        "policies": []
      }
    }
  ]
}
