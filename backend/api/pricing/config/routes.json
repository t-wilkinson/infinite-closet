{
  "routes": [
    {
      "method": "POST",
      "path": "/coupons/discount",
      "handler": "coupon.discount",
      "config": {
        "policies": ["plugins::users-permissions.ratelimit"]
      }
    }
  ]
}
