{
  "routes": [
    {
      "method": "POST",
      "path": "/stripe/payment_intents",
      "handler": "stripe.createPaymentIntent",
      "config": {
        "policies": ["plugins::users-permissions.permissions"]
      }
    },
    {
      "method": "GET",
      "path": "/stripe/payment_methods",
      "handler": "stripe.paymentMethods",
      "config": {
        "policies": ["plugins::users-permissions.permissions"]
      }
    }
  ]
}
