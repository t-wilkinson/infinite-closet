{
  "routes": [
    {
      "method": "GET",
      "path": "/blogs",
      "handler": "blog.find"
    },
    {
      "method": "GET",
      "path": "/blogs/:id",
      "handler": "blog.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
