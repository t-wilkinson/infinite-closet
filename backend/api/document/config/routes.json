{
  "routes": [
    {
      "method": "GET",
      "path": "/documents",
      "handler": "document.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/documents/:id",
      "handler": "document.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
