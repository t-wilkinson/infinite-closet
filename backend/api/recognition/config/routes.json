{
  "routes": [
    {
      "method": "POST",
      "path": "/recognition",
      "handler": "recognition.handleRecognition",
      "config": {
        "policies": ["plugins::users-permissions.permissions"]
      }
    },
    {
      "method": "POST",
      "path": "/notification/RecognitionNotificationService/notify",
      "handler": "recognition.handleRecognitionNotification",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/recognition/filters",
      "handler": "recognition.availableFilters",
      "config": {
        "policies": []
      }
    }
  ]
}
