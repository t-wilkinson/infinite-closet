{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "name": "Order",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "paymentIntent": {
      "type": "string",
      "required": true
    },
    "cartItems": {
      "collection": "cart-item"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "planning",
        "shipping",
        "shipped"
      ],
      "default": "planning"
    },
    "address": {
      "model": "address"
    },
    "shipping": {
      "type": "string"
    },
    "shippingClass": {
      "type": "enumeration",
      "enum": [
        "one_day",
        "next_day",
        "two_day"
      ]
    }
  }
}
