/**
 * @group api
 * @group order/checkout
 */
'use strict'
const userFactory = require('../user/factory')
const orderFactory = require('./factory')

describe('Add order to cart', () => {
})

describe('Action on checkout', () => {
  beforeAll(() => {
    // run onCheckout
  })

  it('moves order stage to planning', () => {
  })
  it('upserts user contact', () => {
  })
  it('sends email', () => {
  })
})
it.todo('checkout')
